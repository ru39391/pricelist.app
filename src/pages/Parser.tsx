import { FC } from 'react';

import Layout from '../components/Layout';
import ParserContent from '../components/ParserContent';

const Parser: FC = () => {
  return <Layout><ParserContent /></Layout>;
};

export default Parser;
